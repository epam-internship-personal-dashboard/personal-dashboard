(function (app) {
    'use strict';

    var cmpt = {
        templateUrl: '/views/register.view.html',
        controllerAs: 'ctrl'
    };

    app.component('pdRegister', cmpt);

}(angular.module('pd.app')));
